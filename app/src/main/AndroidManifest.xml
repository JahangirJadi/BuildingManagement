<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marfarijj.buildingmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.WriteMonthlyExpenses"></activity>
        <activity android:name=".Activities.WriteNoticeActivity" />
        <activity android:name=".Activities.ShowComplainsActivity" />
        <activity android:name=".Activities.ComplainActivity" />
        <activity android:name=".Activities.NoticeActivity" />
        <activity android:name=".Activities.ShowExpensesActivity" />
        <activity android:name=".Activities.MaintenanceCollectionActivity" />
        <activity android:name=".Activities.AdminDashboardActivity" />
        <activity android:name=".Activities.DashboardActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.RegistrationActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>